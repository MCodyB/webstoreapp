<%= form_for(:cart, url: cart_url, method: :put) do |f| %>

  <% @products.each do |product| %>
    <%= product.name %>
    <input type="text" name="cart[<%= product.id %>]" value="0">
    <br>
  <% end %>

  <%= f.submit "Add to cart"%>
<% end %>